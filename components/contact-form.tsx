"use client";import{useState}from"react";export function ContactForm(){const[status,setStatus]=useState<{ok:boolean;message:string}|null>(null),[pending,setPending]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);const form=e.currentTarget;const body=Object.fromEntries(new FormData(form));try{const response=await fetch("/api/contact",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const result=await response.json() as{ok:boolean;message:string};setStatus(result);if(result.ok)form.reset()}catch{setStatus({ok:false,message:"Your message could not be submitted. Please try again."})}finally{setPending(false)}}return <form className="contact-form" onSubmit={submit} noValidate><div className="honeypot" aria-hidden="true"><label>Website<input name="website" tabIndex={-1} autoComplete="off"/></label></div><label>Name <span aria-hidden="true">*</span><input name="name" required maxLength={120} autoComplete="name"/></label><label>Email <span aria-hidden="true">*</span><input name="email" type="email" required maxLength={254} autoComplete="email"/></label><label>Phone Number<input name="phone" type="tel" maxLength={40} autoComplete="tel"/></label><label>Subject <span aria-hidden="true">*</span><input name="subject" required maxLength={200}/></label><label>Message <span aria-hidden="true">*</span><textarea name="message" required maxLength={5000} rows={8}/></label><button className="primary-link" disabled={pending} type="submit">{pending?"Sending…":"Send message"}</button>{status?<p className={status.ok?"contact-success":"contact-error"} role="status" aria-live="polite">{status.message}</p>:null}</form>}
