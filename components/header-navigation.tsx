"use client";
import Image from "next/image"; import Link from "next/link"; import { useState } from "react"; import type { HeaderCategory,HeaderConfig,HeaderItem } from "@/src/header"; import { publicMediaUrl } from "@/src/media-url";
function ManualChildren({parent,items}:{parent:HeaderItem;items:HeaderItem[]}){return <ul>{items.filter(i=>i.visible&&i.parentId===parent.id).sort((a,b)=>a.order-b.order).map(i=><li key={i.id}><Link href={i.destination}>{i.label}</Link></li>)}</ul>}
export function HeaderNavigation({config,categories}:{config:HeaderConfig;categories:HeaderCategory[]}) { const [open,setOpen]=useState(false); const [expanded,setExpanded]=useState<string[]>([]); const toggle=(id:string)=>setExpanded(v=>v.includes(id)?v.filter(x=>x!==id):[...v,id]); const logo=publicMediaUrl(config.branding.logoPath); const manual=config.manualItems.filter(i=>i.visible&&!i.parentId).sort((a,b)=>a.order-b.order); const parents=categories.filter(c=>!c.parentId); const defaults={home:config.defaults.homeVisible?{label:"Home",href:"/"}:null,blog:config.defaults.blogVisible?{label:"Blog",href:"/blog"}:null,contact:config.defaults.contactVisible?{label:"Contact Us",href:null}:null}; return <>{config.announcement.visible&&config.announcement.message?<div className="announcement-bar">{config.announcement.message}</div>:null}<header className="site-header"><Link className="site-brand" href="/" aria-label={`${config.branding.line1} home`}>{logo?<Image src={logo} alt="" width={52} height={52} unoptimized/>:<span className="brand-initial" aria-hidden="true">E</span>}<span>{config.branding.line1}<small>{config.branding.line2}</small></span></Link><button className="mobile-menu-toggle" type="button" aria-expanded={open} aria-controls="primary-menu" onClick={()=>setOpen(!open)}>Menu</button><nav id="primary-menu" className={open?"primary-nav menu-open":"primary-nav"} aria-label="Primary"><ul>{config.defaults.order.map(key=>{const item=defaults[key];if(!item)return null;if(key==="blog")return <li className="has-menu" key={key}><span><Link href="/blog">Blog</Link><button type="button" aria-expanded={expanded.includes("blog")} onClick={()=>toggle("blog")} aria-label="Toggle Blog categories">⌄</button></span><ul className={expanded.includes("blog")?"submenu-open":""}>{parents.map(parent=><li className="has-menu" key={parent.id}><span><Link href={`/categories/${parent.slug}`}>{parent.name}</Link>{categories.some(c=>c.parentId===parent.id)?<button type="button" aria-expanded={expanded.includes(parent.id)} onClick={()=>toggle(parent.id)} aria-label={`Toggle ${parent.name}`}>⌄</button>:null}</span><ul className={expanded.includes(parent.id)?"submenu-open":""}>{categories.filter(c=>c.parentId===parent.id).map(child=><li key={child.id}><Link href={`/categories/${child.slug}`}>{child.name}</Link></li>)}</ul></li>)}</ul></li>;return <li key={key}>{item.href?<Link href={item.href}>{item.label}</Link>:<span className="unresolved-link">{item.label}</span>}</li>})}{manual.map(item=>{const children=config.manualItems.filter(i=>i.visible&&i.parentId===item.id);return <li className={children.length?"has-menu":""} key={item.id}><span><Link href={item.destination}>{item.label}</Link>{children.length?<button type="button" aria-expanded={expanded.includes(item.id)} onClick={()=>toggle(item.id)} aria-label={`Toggle ${item.label}`}>⌄</button>:null}</span>{children.length?<div className={expanded.includes(item.id)?"submenu-open":""}><ManualChildren parent={item} items={config.manualItems}/></div>:null}</li>})}</ul></nav></header></>; }
