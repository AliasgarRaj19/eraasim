const idPattern=/^[A-Za-z0-9_-]{11}$/;export function publicYouTubeId(value:string){let url:URL;try{url=new URL(value)}catch{return null}if(!["http:","https:"].includes(url.protocol))return null;const host=url.hostname.toLowerCase().replace(/^www\./,""),parts=url.pathname.split("/").filter(Boolean);let id:string|null=null;if(host==="youtu.be")id=parts[0]??null;if(["youtube.com","m.youtube.com","youtube-nocookie.com"].includes(host)){if(url.pathname==="/watch")id=url.searchParams.get("v");else if(["embed","shorts","live"].includes(parts[0]))id=parts[1]??null}return id&&idPattern.test(id)?id:null}
