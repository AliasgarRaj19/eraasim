import type{ContactInput}from"@/src/contact-page";export async function notifyContact(recipient:string,message:ContactInput){const url=process.env.CONTACT_EMAIL_WEBHOOK_URL,token=process.env.CONTACT_EMAIL_WEBHOOK_TOKEN;if(!url||!token)return false;let parsed:URL;try{parsed=new URL(url)}catch{return false}if(parsed.protocol!=="https:")return false;try{const response=await fetch(parsed,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${token}`},body:JSON.stringify({to:recipient,replyTo:message.email,subject:`Eraasim contact: ${message.subject}`,text:`Name: ${message.name}\nEmail: ${message.email}\nPhone: ${message.phone??"—"}\n\n${message.message}`}),signal:AbortSignal.timeout(10_000)});if(!response.ok)console.error("Contact notification provider returned a non-success status.");return response.ok}catch(error){console.error("Contact notification failed",error instanceof Error?error.message:"Unknown provider error");return false}}
