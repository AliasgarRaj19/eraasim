type QueryClient={query(text:string,values?:unknown[]):Promise<unknown>};
const safe=(value:string,max:number)=>value.replace(/[\u0000-\u001f\u007f]+/g," ").replace(/\s+/g," ").trim().slice(0,max);
export async function recordContactEmailFailure(client:QueryClient,contactId:string){try{await client.query("INSERT INTO admin_notifications(type,module,title,message,target_type,target_id,target_url,severity,dedupe_key) VALUES('contact_email_failed','contact','Contact notification email failed','A stored contact enquiry could not be delivered by email. Review the message in Contact Messages.','contact_message',$1,$2,'warning',$3) ON CONFLICT DO NOTHING",[contactId,`/contact-messages/${contactId}`,safe(`contact_email_failed:contact_message:${contactId}`,240)])}catch(error){if(!["42P01","42703"].includes((error as{code?:string}).code??""))console.error("Operational notification could not be recorded.")}}
