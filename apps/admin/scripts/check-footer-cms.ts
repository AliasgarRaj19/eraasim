import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{canUsePermission}from"../src/auth/authorization";import{defaultFooterConfig,footerConfigSchema}from"../src/footer/config";
assert(footerConfigSchema.safeParse(defaultFooterConfig).success);assert(footerConfigSchema.safeParse({...defaultFooterConfig,content:{heading:"",description:""}}).success);for(const url of["javascript:alert(1)","data:text/html,x","http://example.com","//example.com","broken"])assert(!footerConfigSchema.safeParse({...defaultFooterConfig,socialLinks:defaultFooterConfig.socialLinks.map((link,index)=>index?link:{...link,visible:true,url})}).success);assert(footerConfigSchema.safeParse({...defaultFooterConfig,socialLinks:defaultFooterConfig.socialLinks.map((link,index)=>index?link:{...link,visible:true,url:"https://example.com/eraasim"})}).success);
const navigation=readFileSync(new URL("../src/navigation/admin-navigation.ts",import.meta.url),"utf8");assert(navigation.includes('{ label: "Footer", href: "/footer/content", permission: "footer.view" }')&&!navigation.includes('label: "Social Media Links"')&&!navigation.includes('label: "Content & Directory"'));const socialRoute=readFileSync(new URL("../app/(admin)/footer/social/page.tsx",import.meta.url),"utf8");assert(socialRoute.includes('requirePermission("footer.view")')&&socialRoute.includes('redirect("/footer/content#social-links")'));
const form=readFileSync(new URL("../components/footer-form.tsx",import.meta.url),"utf8");assert(form.includes("Category Directory / Footer 2")&&form.includes("Page Navigation")&&form.includes("Footer Content")&&form.includes("Social Links")&&form.includes("Save Draft")&&form.includes("Publish Changes"));assert(form.includes("No Pages are available for Footer navigation. Create and publish a Page first.")&&!form.includes("Page label")&&!form.includes("Page URL"));const action=readFileSync(new URL("../app/(admin)/footer/content/actions.ts",import.meta.url),"utf8");assert(action.includes('.for("update")')&&action.includes("current.published")&&action.includes("footer.draft_saved")&&action.includes("footer.published")&&action.includes("not publicly eligible"));const master={isMasterAdmin:true,permissionKeys:new Set<string>()};for(const permission of["footer.view","footer.edit","footer.publish"])assert(canUsePermission(master,permission));console.log("PASS: one Footer navigation entry, unified editor, authorized legacy redirect, Page empty state, Social preservation, Draft/Publish, stale locking, and permissions verified.");
