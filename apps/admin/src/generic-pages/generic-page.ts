import { z } from "zod";import { isValidSlug, slugify } from "@/src/blog/slug";
export const RESERVED_PAGE_SLUGS=new Set(["about","admin","analytics","api","blog","categories","contact-us","dashboard","footer","header","login","logs","media","pages","sitemap","staff","theme","uploads"]);export const genericPageInputSchema=z.object({title:z.string().trim().min(1,"Title is required.").max(200),slug:z.string().trim().min(1).max(180),content:z.string().min(1),featuredImagePath:z.string().trim().max(500).optional(),featuredImageIntent:z.enum(["keep","replace","remove"]).default("keep"),featuredImageAlt:z.string().trim().max(300).optional(),seoTitle:z.string().trim().max(200).optional(),seoDescription:z.string().trim().max(500).optional(),intent:z.enum(["preserve","draft","published","unpublished"])});export function parseGenericPageForm(formData:FormData){return genericPageInputSchema.safeParse(Object.fromEntries(formData))}export function normalizeGenericPageSlug(value:string){return slugify(value)}export function genericPageSlugError(value:string){if(!isValidSlug(value))return"Use lowercase letters, numbers, and single hyphens only.";if(RESERVED_PAGE_SLUGS.has(value))return"This slug is reserved for an Eraasim system route.";return null}export function genericPagePublishing(current:{status:"draft"|"published"|"unpublished";publishedAt:Date|null},intent:"preserve"|"draft"|"published"|"unpublished",now=new Date()){if(intent==="preserve")return current;if(intent==="published")return{status:"published" as const,publishedAt:current.status==="published"&&current.publishedAt?current.publishedAt:now};return{status:intent,publishedAt:current.publishedAt}}
