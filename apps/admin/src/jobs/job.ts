import{z}from"zod";import{isValidSlug,slugify}from"@/src/blog/slug";export const jobInputSchema=z.object({title:z.string().trim().min(1,"Job Title is required.").max(200),slug:z.string().trim().min(1).max(180),shortDescription:z.string().trim().min(1,"Short Description is required.").max(500),description:z.string().min(1),location:z.string().trim().max(200).optional(),employmentType:z.string().trim().max(120).optional(),department:z.string().trim().max(120).optional(),experience:z.string().trim().max(200).optional(),seoTitle:z.string().trim().max(200).optional(),seoDescription:z.string().trim().max(500).optional(),intent:z.enum(["preserve","draft","published","closed"])});export function parseJobForm(formData:FormData){return jobInputSchema.safeParse(Object.fromEntries(formData))}export function normalizeJobSlug(value:string){return slugify(value)}export function jobSlugError(value:string){return isValidSlug(value)?null:"Use lowercase letters, numbers, and single hyphens only."}export function jobPublishing(current:{status:"draft"|"published"|"closed";publishedAt:Date|null;closedAt:Date|null},intent:"preserve"|"draft"|"published"|"closed",now=new Date()){if(intent==="preserve")return{status:current.status,publishedAt:current.publishedAt,closedAt:current.closedAt};if(intent==="published")return{status:"published"as const,publishedAt:current.status==="published"&&current.publishedAt?current.publishedAt:now,closedAt:null};if(intent==="closed")return{status:"closed"as const,publishedAt:current.publishedAt,closedAt:now};return{status:"draft"as const,publishedAt:current.publishedAt,closedAt:null}}
