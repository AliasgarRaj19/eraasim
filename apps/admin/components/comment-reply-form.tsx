"use client";import{useActionState}from"react";import{replyToComment}from"@/app/(admin)/comments/actions";export function CommentReplyForm({id}:{id:string}){const[state,action,pending]=useActionState(replyToComment,{});return <form action={action}><input type="hidden" name="commentId" value={id}/><label>Reply text<textarea name="reply" required maxLength={5000} rows={6}/></label>{state.error?<p className="form-error">{state.error}</p>:null}<button className="primary-action" disabled={pending}>Add Admin Reply</button></form>}
