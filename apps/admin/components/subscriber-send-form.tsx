"use client";export function SubscriberSendForm({postId,previouslySent,action}:{postId:string;previouslySent:boolean;action:(f:FormData)=>void}){return <form action={action} onSubmit={e=>{const text=previouslySent?"This post has previously been sent to subscribers. Send again?":"Send this post notification to all active subscribers?";if(!window.confirm(text))e.preventDefault()}}><input type="hidden" name="postId" value={postId}/><button className="primary-action">Send to Subscribers</button></form>}
