"use client";import{useActionState}from"react";import{inviteStaff,type StaffActionState}from"@/app/(admin)/staff/actions";const initial:StaffActionState={};export function StaffInvitationForm(){const[state,action,pending]=useActionState(inviteStaff,initial);return <form className="cms-form staff-form" action={action}><label>Email Address<input name="email" type="email" autoComplete="email" required maxLength={254}/></label><label>Role <small>Optional label only; it grants no permissions.</small><input name="roleLabel" maxLength={80} placeholder="Editor, Moderator, HR…"/></label>{state.error?<p className="form-error" role="alert">{state.error}</p>:null}{state.success?<p className="success-message" role="status">{state.success}</p>:null}<button disabled={pending}>{pending?"Sending…":"Send Invitation"}</button></form>}
