"use client";import{useActionState}from"react";import{resetStaffPassword,type ResetState}from"@/app/staff/reset-password/[token]/actions";export function StaffResetForm({token}:{token:string}){const[state,action,pending]=useActionState(resetStaffPassword,{}as ResetState);return <form action={action}><input type="hidden" name="token" value={token}/><label>New Password<input name="password" type="password" autoComplete="new-password" minLength={16} maxLength={256} required/></label><label>Confirm Password<input name="confirmPassword" type="password" autoComplete="new-password" minLength={16} maxLength={256} required/></label>{state.error?<p className="error" role="alert">{state.error}</p>:null}<button disabled={pending}>{pending?"Saving…":"Reset Password"}</button></form>}
