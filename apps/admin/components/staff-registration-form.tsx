"use client";import{useActionState}from"react";import{registerStaff,type RegistrationState}from"@/app/staff/register/[token]/actions";export function StaffRegistrationForm({token,email}:{token:string;email:string}){const[state,action,pending]=useActionState(registerStaff,{}as RegistrationState);return <form action={action}><input type="hidden" name="token" value={token}/><label>Full Name<input name="name" autoComplete="name" minLength={2} maxLength={120} required/></label><label>Email<input value={email} readOnly aria-readonly="true"/></label><label>Password<input name="password" type="password" autoComplete="new-password" minLength={16} maxLength={256} required/><small>At least 16 characters with uppercase, lowercase, number, and symbol.</small></label><label>Confirm Password<input name="confirmPassword" type="password" autoComplete="new-password" minLength={16} maxLength={256} required/></label><label className="terms-check"><input name="terms" type="checkbox" required/> I accept the Terms and Conditions.</label>{state.error?<p className="error" role="alert">{state.error}</p>:null}<button disabled={pending}>{pending?"Creating Account…":"Create Account"}</button></form>}
