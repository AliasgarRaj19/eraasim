import{eq}from"drizzle-orm";import{saveLogSettings}from"@/app/(admin)/logs/settings/actions";import{LogRetentionForm}from"@/components/log-retention-form";import{requirePermission}from"@/src/auth/authorization";import{db}from"@/src/db";import{activityLogSettings}from"@/src/db/schema";export default async function LogsSettings({searchParams}:{searchParams:Promise<{saved?:string}>}){await requirePermission("logs.settings");const[[settings],query]=await Promise.all([db.select().from(activityLogSettings).where(eq(activityLogSettings.id,"global")).limit(1),searchParams]);const current=settings?.retentionMonths??3;return <section className="home-cms-page"><div className="page-heading"><p className="page-eyebrow">Logs</p><h1>Activity Log Retention</h1><p>Activity logs are currently retained for {current} months.</p></div>{query.saved?<p className="success-message">Settings saved. Cleanup will apply the policy on its next daily run.</p>:null}<LogRetentionForm current={current} action={saveLogSettings}/></section>}
