import{CommentsSettingsForm}from"@/components/comments-settings-form";import{requirePermission}from"@/src/auth/authorization";import{defaultCommentsSettings,parseCommentsSettings}from"@/src/comments/config";import{db}from"@/src/db";import{commentsSettings}from"@/src/db/schema";export default async function Page({searchParams}:{searchParams:Promise<{saved?:string}>}){await requirePermission("comments.settings");const[stored]=await db.select().from(commentsSettings).limit(1),parsed=parseCommentsSettings(stored),query=await searchParams;return <CommentsSettingsForm settings={parsed.success?parsed.data:defaultCommentsSettings} saved={query.saved==="1"}/>}
