import Link from"next/link";import{count,desc}from"drizzle-orm";import{requireRouteAccess}from"@/src/auth/authorization";import{db}from"@/src/db";import{contactMessages}from"@/src/db/schema";const size=20;const fmt=new Intl.DateTimeFormat("en-IN",{dateStyle:"medium",timeStyle:"short",timeZone:"Asia/Kolkata"});
export default async function Page({searchParams}:{searchParams:Promise<{page?:string}>}){await requireRouteAccess("/contact-messages");const q=await searchParams,page=Math.max(1,Number.parseInt(q.page??"1",10)||1);const[[total],rows]=await Promise.all([db.select({value:count()}).from(contactMessages),db.select().from(contactMessages).orderBy(desc(contactMessages.submittedAt),desc(contactMessages.id)).limit(size).offset((page-1)*size)]);const pages=Math.max(1,Math.ceil(total.value/size));return <section><div className="page-heading"><p className="page-eyebrow">Inbox</p><h1>Contact Messages</h1><p>Stored enquiries, newest first. Email is a notification only.</p></div>{rows.length?<><div className="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Subject</th><th>Submitted</th><th>Status</th><th>Notification</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td><Link href={`/contact-messages/${row.id}`}>{row.name}</Link></td><td>{row.email}</td><td>{row.subject}</td><td>{fmt.format(row.submittedAt)}</td><td><span className={`status-pill status-${row.status}`}>{row.status}</span></td><td>{row.notificationSent?"Sent":"Not sent"}</td></tr>)}</tbody></table></div><nav className="pagination" aria-label="Contact messages pages">{page>1?<Link href={`/contact-messages?page=${page-1}`}>Previous</Link>:<span/>}<span>Page {page} of {pages}</span>{page<pages?<Link href={`/contact-messages?page=${page+1}`}>Next</Link>:<span/>}</nav></>:<div className="empty-state"><h2>No contact messages</h2><p>New enquiries will appear here after they are securely stored.</p></div>}</section>}
