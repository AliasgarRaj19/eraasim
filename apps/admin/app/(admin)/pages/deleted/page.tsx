import{desc,isNotNull}from"drizzle-orm";import{changeGenericPageLifecycle}from"@/app/(admin)/pages/actions";import{canUsePermission,requirePermission}from"@/src/auth/authorization";import{db}from"@/src/db";import{genericPages}from"@/src/db/schema";export default async function Deleted(){const{authorization}=await requirePermission("pages.generic.view");const rows=await db.select().from(genericPages).where(isNotNull(genericPages.deletedAt)).orderBy(desc(genericPages.deletedAt)).limit(200);return <section><div className="page-heading"><p className="page-eyebrow">Pages</p><h1>Deleted Pages</h1><p>Restored Pages return as Unpublished for safe review.</p></div>{rows.length?<div className="table-wrap"><table><thead><tr><th>Title</th><th>Slug</th><th>Prior status</th><th>Actions</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td>{row.title}</td><td>/{row.slug}</td><td>{row.status}</td><td className="table-actions">{canUsePermission(authorization,"pages.generic.delete")?<><form action={changeGenericPageLifecycle}><input type="hidden" name="pageId" value={row.id}/><button name="action" value="restore">Restore as Unpublished</button></form><form action={changeGenericPageLifecycle}><input type="hidden" name="pageId" value={row.id}/><button name="action" value="permanent-delete">Delete Permanently</button></form></>:null}</td></tr>)}</tbody></table></div>:<div className="public-empty"><h2>No deleted Pages</h2></div>}</section>}
