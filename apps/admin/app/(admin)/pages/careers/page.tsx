import{eq}from"drizzle-orm";import{CareersPageForm}from"@/components/careers-page-form";import{canUsePermission,requireRouteAccess}from"@/src/auth/authorization";import{defaultCareersConfig,parseCareersConfig}from"@/src/careers/config";import{db}from"@/src/db";import{careersPageConfigurations}from"@/src/db/schema";export default async function Page({searchParams}:{searchParams:Promise<{result?:string}>}){const{authorization}=await requireRouteAccess("/pages/careers"),[[row],query]=await Promise.all([db.select({draft:careersPageConfigurations.draft,draftVersion:careersPageConfigurations.draftVersion}).from(careersPageConfigurations).where(eq(careersPageConfigurations.id,"careers")).limit(1),searchParams]),parsed=parseCareersConfig(row?.draft);return <CareersPageForm config={parsed.success?parsed.data:defaultCareersConfig} version={row?.draftVersion??0} canEdit={canUsePermission(authorization,"pages.careers.edit")} canPublish={canUsePermission(authorization,"pages.careers.publish")} result={query.result}/>}
