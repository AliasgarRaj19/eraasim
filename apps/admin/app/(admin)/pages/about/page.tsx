import{eq}from"drizzle-orm";import{AboutPageForm}from"@/components/about-page-form";import{canUsePermission,requireRouteAccess}from"@/src/auth/authorization";import{db}from"@/src/db";import{aboutPageConfigurations}from"@/src/db/schema";import{defaultAboutConfig,parseAboutConfig}from"@/src/about-page/config";
export default async function Page({searchParams}:{searchParams:Promise<{result?:string}>}){const{authorization}=await requireRouteAccess("/pages/about");const[[row],query]=await Promise.all([db.select({draft:aboutPageConfigurations.draft,draftVersion:aboutPageConfigurations.draftVersion}).from(aboutPageConfigurations).where(eq(aboutPageConfigurations.id,"about")).limit(1),searchParams]);const parsed=parseAboutConfig(row?.draft);return <AboutPageForm config={parsed.success?parsed.data:defaultAboutConfig} version={row?.draftVersion??0} canEdit={canUsePermission(authorization,"pages.about.edit")} canPublish={canUsePermission(authorization,"pages.about.publish")} result={query.result}/>}
