import{NextResponse}from"next/server";import{authorizeAnyPermission}from"@/src/auth/authorization";import{MAX_IMAGE_BYTES,saveAboutImage}from"@/src/uploads/storage";export const runtime="nodejs";
export async function POST(request:Request){const access=await authorizeAnyPermission(["pages.about.edit"]);if(!access.authorized)return NextResponse.json({error:access.status===401?"Authentication is required.":"You do not have permission to upload an About image."},{status:access.status});try{const formData=await request.formData(),file=formData.get("image");if(!(file instanceof File))return NextResponse.json({error:"Select an image to upload."},{status:400});if(file.size>MAX_IMAGE_BYTES)return NextResponse.json({error:"Images must be no larger than 5 MB."},{status:413});return NextResponse.json(await saveAboutImage(file),{status:201})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"About image upload failed."},{status:400})}}
