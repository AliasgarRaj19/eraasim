import{and,eq,gt}from"drizzle-orm";import{StaffResetForm}from"@/components/staff-reset-form";import{db}from"@/src/db";import{staffPasswordResets}from"@/src/db/schema";import{tokenHash}from"@/src/staff/invitations";export default async function Reset({params}:{params:Promise<{token:string}>}){const{token}=await params,[valid]=token.length<=200?await db.select({id:staffPasswordResets.id}).from(staffPasswordResets).where(and(eq(staffPasswordResets.tokenHash,tokenHash(token)),eq(staffPasswordResets.status,"pending"),gt(staffPasswordResets.expiresAt,new Date()))).limit(1):[];return <main className="shell"><section className="card"><p className="eyebrow">Staff Security</p><h1>Reset Password</h1>{valid?<StaffResetForm token={token}/>:<p className="error">This password reset link is invalid, expired, revoked, or already used.</p>}</section></main>}
