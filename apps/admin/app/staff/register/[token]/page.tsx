import{and,eq,gt}from"drizzle-orm";import{StaffRegistrationForm}from"@/components/staff-registration-form";import{db}from"@/src/db";import{staffInvitations}from"@/src/db/schema";import{tokenHash}from"@/src/staff/invitations";export default async function Register({params}:{params:Promise<{token:string}>}){const{token}=await params,[invitation]=token.length<=200?await db.select({email:staffInvitations.email}).from(staffInvitations).where(and(eq(staffInvitations.tokenHash,tokenHash(token)),eq(staffInvitations.status,"pending"),gt(staffInvitations.expiresAt,new Date()))).limit(1):[];return <main className="shell"><section className="card"><p className="eyebrow">Staff Registration</p><h1>Join Eraasim</h1>{invitation?<><p className="lede">Complete your staff account registration. Your administrator assigns access separately.</p><StaffRegistrationForm token={token} email={invitation.email}/></>:<p className="error">This invitation is invalid, expired, revoked, or already used.</p>}</section></main>}
