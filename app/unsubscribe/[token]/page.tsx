import{getPool}from"@/src/db";import{hashToken}from"@/src/subscribers";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{token:string}>}){const{token}=await params;if(token.length>=20&&token.length<=200)try{await getPool().query("UPDATE subscribers SET status='unsubscribed',unsubscribed_at=COALESCE(unsubscribed_at,CURRENT_TIMESTAMP),updated_at=CURRENT_TIMESTAMP WHERE unsubscribe_token_hash=$1",[hashToken(token)])}catch{/* Safe identical result before migration or on invalid token. */}return <section className="unsubscribe-page"><h1>You have been unsubscribed.</h1><p>You will not receive future Eraasim story notifications.</p></section>}
