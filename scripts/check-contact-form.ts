import{strict as assert}from"node:assert";import{acceptContactRate,parsePublicContact,validateContactInput,defaultContactConfig}from"../src/contact-page";const valid={name:" A ",email:"A@example.com",phone:"",subject:"Hello",message:"Plain text"};assert.deepEqual(validateContactInput(valid),{name:"A",email:"a@example.com",phone:null,subject:"Hello",message:"Plain text"});assert.equal(validateContactInput({...valid,email:"bad"}),null);assert.equal(validateContactInput({...valid,message:"<script>x</script>"}),null);assert.equal(validateContactInput({...valid,name:"x".repeat(121)}),null);assert.equal(parsePublicContact(null).form.visible,false);const key=`regression-${Date.now()}`;for(let i=0;i<5;i++)assert.equal(acceptContactRate(key,1),true);assert.equal(acceptContactRate(key,1),false);assert.equal(acceptContactRate(key,700_001),true);assert.equal(defaultContactConfig.form.successMessage.length>0,true);console.log("Contact form regression passed.");
