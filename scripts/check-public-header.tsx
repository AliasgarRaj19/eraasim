import assert from "node:assert/strict"; import { readFileSync } from "node:fs"; import { renderToStaticMarkup } from "react-dom/server"; import { HeaderNavigation } from "../components/header-navigation"; import { defaultHeaderConfig,parsePublicHeader } from "../src/header";
assert.deepEqual(parsePublicHeader(null),defaultHeaderConfig);assert.deepEqual(parsePublicHeader({bad:true}),defaultHeaderConfig);
const categories=[{id:"p",name:"Parent",slug:"parent",parentId:null},{id:"c",name:"Child",slug:"child",parentId:"p"}];const html=renderToStaticMarkup(<HeaderNavigation config={{...defaultHeaderConfig,announcement:{visible:true,message:"News"},manualItems:[{id:"123e4567-e89b-42d3-a456-426614174000",label:"About",destination:"/about",parentId:null,visible:true,order:0},{id:"123e4567-e89b-42d3-a456-426614174001",label:"Hidden",destination:"/hidden",parentId:null,visible:false,order:1}]}} categories={categories}/>);assert(html.includes("announcement-bar")&&html.includes("News")&&html.includes("/categories/parent")&&html.includes("/categories/child")&&html.includes("/about")&&!html.includes("/hidden")&&html.includes("aria-expanded"));const hidden=renderToStaticMarkup(<HeaderNavigation config={{...defaultHeaderConfig,defaults:{...defaultHeaderConfig.defaults,blogVisible:false},announcement:{visible:false,message:""}}} categories={categories}/>);assert(!hidden.includes("announcement-bar")&&!hidden.includes("/categories/parent"));
const source=readFileSync(new URL("../src/header.ts",import.meta.url),"utf8");assert(source.includes("FROM categories ORDER BY lower(name)")&&source.includes("SELECT published FROM header_configurations")&&source.includes("LIMIT 500"));const layout=readFileSync(new URL("../app/layout.tsx",import.meta.url),"utf8");assert(layout.includes("<PublicHeader />"));const css=readFileSync(new URL("../app/globals.css",import.meta.url),"utf8");assert(css.includes(".mobile-menu-toggle")&&css.includes("prefers-reduced-motion")&&css.includes("focus-within"));console.log("PASS: fallback, branding, announcement collapse, exact dynamic Parent/Child URLs, hidden menus, shared all-route header, desktop focus, and mobile aria-expanded architecture verified.");
